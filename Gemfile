source "https://rubygems.org"

# Use apenas o meta-pacote do GitHub Pages (ele pinará as versões certas)
gem "github-pages", group: :jekyll_plugins

# Necessário no Ruby 3+ para rodar localmente `jekyll serve`
gem "webrick", "~> 1.8"
